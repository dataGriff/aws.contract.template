# The package publishes to (and consumers install from) GitHub Packages.
# NODE_AUTH_TOKEN: GITHUB_TOKEN in Actions; a PAT with read:packages locally
# (only needed to INSTALL published versions — developing this repo needs none).
@datagriff:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
